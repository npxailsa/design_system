import { Meta, Controls, Canvas } from "@storybook/addon-docs/blocks";
import * as AlertStories from "./Alert.stories";

<Meta of={AlertStories} title="Components/Alert" />

# Alert

The `Alert` component is used to communicate important information to users through colour-coded banners. It supports 48 total variants across 3 sizes, 8 colour themes, and 2 border states, with optional leading icon, trailing close button, and description subtext.

## Design Tokens

We use the following tokens to render our alerts:

### Layout Tokens

| Token Name | Description |
| :--- | :--- |
| `--alert-padding-small` | Padding for small size (6px 8px) |
| `--alert-padding-default` | Padding for default size (8px 12px) |
| `--alert-padding-large` | Padding for large size (10px 12px) |
| `--alert-gap` | Gap between icon-group and close button (12px) |
| `--alert-icon-text-gap` | Gap between leading icon and text (8px) |
| `--alert-text-gap-small` | Gap between title and description — small (2px) |
| `--alert-text-gap-default` | Gap between title and description — default (2px) |
| `--alert-text-gap-large` | Gap between title and description — large (1px) |
| `--alert-border-radius` | Border radius for bordered variant (4px) |
| `--alert-border-width` | Border width (1px) |
| `--alert-icon-opacity` | Opacity applied to leading and close icons (0.5) |
| `--alert-min-width` | Minimum width of the alert (400px) |

### Typography Tokens

| Token Name | Description |
| :--- | :--- |
| `--alert-font-family` | Font family (F37 Ginger Pro) |
| `--alert-letter-spacing` | Letter spacing (0.15px) |
| `--alert-title-weight` | Title font weight (400 / regular) |
| `--alert-desc-weight` | Description font weight (300 / light) |
| `--alert-description-color` | Description text color (gray-700) |
| `--alert-title-size-small` | Title font size — small (12px) |
| `--alert-title-size-default` | Title font size — default (14px) |
| `--alert-title-size-large` | Title font size — large (16px) |

### Icon Size Tokens

| Token Name | Description |
| :--- | :--- |
| `--alert-leading-icon-small` | Leading icon — small (16px) |
| `--alert-leading-icon-default` | Leading icon — default (22px) |
| `--alert-leading-icon-large` | Leading icon — large (28px) |
| `--alert-close-icon-small` | Close icon — small (14px) |
| `--alert-close-icon-default` | Close icon — default (16px) |
| `--alert-close-icon-large` | Close icon — large (18px) |

### Colour Variant Tokens

| Token Name | Description |
| :--- | :--- |
| `--alert-bg-default` | Default background — rgba(58,50,130,0.05) |
| `--alert-title-default` | Default title color — secondary-navy |
| `--alert-bg-warning` | Warning background — status-orange-light |
| `--alert-title-warning` | Warning title color — status-dark-orange |
| `--alert-icon-warning` | Warning leading icon fill — status-orange |
| `--alert-bg-light-gray` | Light-gray background — neutral-gray-100 |
| `--alert-bg-navy` | Navy background — status-info-blue-light |
| `--alert-bg-success` | Success background — status-green-light |
| `--alert-bg-error` | Error background — status-red-light |
| `--alert-bg-purple` | Purple background — status-purple-light |
| `--alert-bg-white` | White background — base-white |

## Principles

**Communicate with clarity** — Use the appropriate colour variant to match the semantic meaning: error for failures, success for confirmations, warning for caution, and navy for informational messages.

**Use sparingly and intentionally** — Alerts demand attention. Overusing them diminishes their impact. Reserve alerts for messages that genuinely require user awareness.

**Provide actionable context** — Always pair the title with a helpful description when additional context aids user understanding.

**Respect dismissibility** — When an alert includes a close button, ensure the onClose callback properly removes the alert from the DOM.

## Using the Component

```tsx
import { Alert } from './Alert';

// Basic bordered alert
<Alert
  size="default"
  colour="success"
  border
  title="Changes saved successfully"
  description="Your profile has been updated."
  onClose={() => console.log('dismissed')}
/>

// Minimal alert without description or icons
<Alert
  colour="warning"
  border={false}
  title="Session expiring soon"
  showSubtext={false}
  showLeadingIcon={false}
  showTrailingIcon={false}
/>
```

The component accepts the following props:

<Controls />

## Visual Style

Alerts use colour-coded backgrounds, borders, and icons to communicate message severity at a glance.

### Colour Variants

<Canvas of={AlertStories.Colours} />

### Border vs. No Border

<Canvas of={AlertStories.NoBorder} />

### Without Description

<Canvas of={AlertStories.WithoutDescription} />

### Without Icons

<Canvas of={AlertStories.WithoutIcons} />

## Size and spacing

The Alert component supports three sizes. Choose the size that best fits the context and information density.

<Canvas of={AlertStories.Sizes} />

## Leading Icons

Each colour variant uses a specific NPX custom SVG icon to reinforce semantic meaning:

| Colour | Icon |
| :--- | :--- |
| `default` | Bullseye (concentric rings target) |
| `warning` | Alert triangle (triangle with exclamation) |
| `light-gray` | Bullseye (concentric rings target) |
| `navy` | Information circle (i inside circle) |
| `success` | Check bold (bold checkmark) |
| `error` | Close thick (thick X cross) |
| `purple` | File document (document with lines) |
| `white` | Bullseye (concentric rings target) |

## Full Variant Matrix

This component provides 48 possible variants: 3 sizes × 8 colours × 2 border states.

<Canvas of={AlertStories.AllVariants} />

## Contribution

When proposing updates or new properties for the Alert component, consider the following:

1. Does the new colour variant have a clear semantic purpose?
2. Does it maintain accessibility contrast ratios for both bordered and non-bordered states?
3. Are the leading icon and colour pairing intuitive for the message type?

To contribute a new variation, please bring it to our Biweekly Design System meeting for review with the whole team.
