import { Meta, Controls, Canvas } from "@storybook/addon-docs/blocks";
import * as LabelStories from "./Label.stories";

<Meta of={LabelStories} title="Foundations/Label" />

# Label

The `Label` component is a core building block of the design system, used to display text with optional icons and dropdown split functionality. It is designed to be highly systematic, with 120 possible variants based on size, weight, alignment, and state.

## Usage

```tsx
import { Label } from './Label';
import PersonIcon from '@mui/icons-material/Person';

const MyComponent = () => (
  <Label size="default" weight="bold" leadingIcon={PersonIcon}>
    Example Label
  </Label>
);
```

## API

The component accepts the following props:

<Controls />

## Examples

### Default

<Canvas of={LabelStories.Default} />

### Sizes

<Canvas of={LabelStories.Sizes} />

### Weights

<Canvas of={LabelStories.Weights} />

### With Dropdown

<Canvas of={LabelStories.WithDropdown} />

### Alignment

<Canvas of={LabelStories.Alignments} />

## Systematic Variants

This section showcases the systematic application of variants across all 5 sizes, 4 weights, 3 alignments, and 2 dropdown states.

### Full Matrix (120 Variants)

<Canvas of={LabelStories.AllVariants} />

## Design System Integration

The component is fully integrated with the NPX Design System token system.

### Design Tokens

The component uses the following design tokens for styling, which are defined in `src/styles/tokens.css`:

#### Core Tokens

| Token Name | Description | Value |
| :--- | :--- | :--- |
| `--label-text-color` | Primary text color | `var(--brand-text-main)` |
| `--label-font-family` | Font family | `var(--brand-font-primary)` |
| `--label-icon-color` | Primary icon color | `var(--brand-icon-main)` |
| `--label-divider-color` | Dropdown divider color | `var(--brand-border-main)` |
| `--label-letter-spacing` | Global letter spacing | `var(--global-type-letter-spacing-primary-label)` |

#### Size Tokens (Font Size / Line Height / Gap)

| Token Name | Description | Value |
| :--- | :--- | :--- |
| `--label-size-large` | Large font size | `var(--global-type-size-primary-label-lg)` |
| `--label-size-default` | Default font size | `var(--global-type-size-primary-label)` |
| `--label-size-small` | Small font size | `var(--global-type-size-primary-label-sm)` |
| `--label-size-x-small` | X-Small font size | `var(--global-type-size-primary-label-xs)` |
| `--label-size-2x-small` | 2X-Small font size | `var(--global-type-size-primary-label-xxs)` |
| `--label-line-height-large` | Large line height | `var(--global-type-line-height-label-lg)` |
| `--label-line-height-default` | Default line height | `var(--global-type-line-height-label)` |
| `--label-line-height-small` | Small line height | `var(--global-type-line-height-label-sm)` |
| `--label-line-height-x-small` | X-Small line height | `var(--global-type-line-height-label-xs)` |
| `--label-line-height-2x-small` | 2X-Small line height | `var(--global-type-line-height-label-xxs)` |
| `--label-gap-large` | Large gap between icons/text | `var(--global-spacing-sizing-8px)` |
| `--label-gap-default` | Default gap between icons/text | `var(--global-spacing-sizing-6px)` |
| `--label-gap-small` | Small gap between icons/text | `var(--global-spacing-sizing-4px)` |
| `--label-gap-x-small` | X-Small gap between icons/text | `var(--global-spacing-sizing-4px)` |
| `--label-gap-2x-small` | 2X-Small gap between icons/text | `var(--global-spacing-sizing-2px)` |

#### Weight Tokens

| Token Name | Description | Value |
| :--- | :--- | :--- |
| `--label-weight-bold` | Bold font weight | `var(--global-type-weight-bold)` |
| `--label-weight-medium` | Medium font weight | `var(--global-type-weight-medium)` |
| `--label-weight-light` | Light font weight | `var(--global-type-weight-light)` |
| `--label-weight-thin` | Thin font weight | `var(--global-type-weight-thin)` |

#### Dropdown Specific Tokens

| Token Name | Description | Value |
| :--- | :--- | :--- |
| `--label-dropdown-split-width` | Divider width | `var(--global-spacing-stroke-1px)` |
| `--label-dropdown-split-opacity` | Divider opacity | `0.5` |
| `--label-dropdown-chevron-opacity` | Chevron icon opacity | `0.6` |
| `--label-dropdown-split-gap` | Gap within dropdown split | `var(--global-spacing-sizing-4px)` |
| `--label-dropdown-padding-left` | Padding left for split section | `var(--global-spacing-sizing-2px)` |

## Guidelines

### Best Practices
- Use the `size` prop to ensure consistent scaling.
- Ensure appropriate contrast ratios when switching between brands.
- Use component tokens for overrides rather than hardcoded values.

### Accessibility
- Ensure labels have sufficient color contrast (WCAG 2.1 AA).
- If an icon carries unique meaning, ensure appropriate ARIA labels are provided via children or parent components.

### Dos and Don'ts
- **Do:** Use systematic weights (bold, medium, light, thin) for consistent hierarchy.
- **Don't:** Overwrite internal component styles directly; use the token system.
