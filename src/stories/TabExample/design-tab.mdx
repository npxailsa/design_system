import { Meta } from "@storybook/addon-docs";

<Meta
  id="design-tab-example"
  title="hidden/TabExample/Design"
/>

# Design Tab

This is the design tab content. It can include information about the visual aspects of the component.

- **Color Palette**: Uses `--color-primary`.
- **Typography**: Uses `var(--font-family-primary)`.
